dataSource:
  postgres:
    test:
      connection: 'postgresql://postgres:postgres@localhost:5432/wj-dev?schema=public'
      logStatement: true

model:
  schemaSet:
    test:
      dataSource: postgres.test